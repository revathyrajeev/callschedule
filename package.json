{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js", // Your main file
  "scripts": {
    "start": "node index.js", // Start command added
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.0.6",
    "express": "^4.17.1", // Ensure to add express
    "nodemailer": "^6.4.18", // Ensure to add nodemailer
    "dotenv": "^10.0.0" // Ensure to add dotenv
  },
  "engines": {
    "node": "18.x" // Specify your Node.js version
  }
}
