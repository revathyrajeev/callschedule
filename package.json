{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js", // This should be your main server file (e.g., server.js)
  "scripts": {
    "start": "node index.js", // Add this line
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.0.6",
    "express": "^4.17.1", // Add Express or any other dependencies your app uses
    "nodemailer": "^6.4.18", // Add Nodemailer for email functionality
    "dotenv": "^10.0.0" // Add dotenv for environment variables
  },
  "engines": {
    "node": "18.x" // Specify the Node.js version you are using
  }
}

